<%- include('../template/header.ejs', {
    page_title: page_title,
    flag : flag
}) %>

<!-- discord-like:
    sidebar for quick access to:
        home page (website logo)
        functions section:
            new file: create a new file stored online only (can be saved afterwards)
            open file: open a file from either the online repo or a local one
            save file: only available if working on a file
            download file: only available if working on a file
        divider
        rooms section (which is scrollable)
        then almost at the bottom a divider
        cookies usage, privacy etc... 
-->

<%- include('../template/footer.ejs') %>